{
  "name": "sequential-thinking-mcp",
  "version": "1.0.0",
  "type": "module",
  "engines": {
    "node": "18.x"  // Render 默认 Node.js 版本
  },
  "scripts": {
    "start": "node src/sequentialthinking/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "supergateway": "^1.0.2",  // ✅ 改为可用版本
    "zod": "^3.22.4"
  }
}
