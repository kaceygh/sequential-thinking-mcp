{
  "name": "sequential-thinking-mcp",
  "version": "1.0.0",
  "description": "Sequential Thinking MCP Server for dcdeploy",
  "private": true,
  "engines": {
    "node": "20.x"
  },
  "dependencies": {
    "supergateway": "^1.0.0"
  },
  "scripts": {
    "start": "supergateway --stdio \"npx -y @modelcontextprotocol/server-sequential-thinking\" --port $PORT --healthPath /health --ssePath /sse"
  }
}
